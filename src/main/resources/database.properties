# EthioTour Database Configuration
# Switch between IN_MEMORY and POSTGRESQL modes

# Mode: IN_MEMORY, SQLITE, or POSTGRESQL
db.mode=POSTGRESQL

# SQLite Configuration
db.sqlite.path=ethiotour.db

# PostgreSQL Configuration
db.postgresql.url=jdbc:postgresql://localhost:5432/swing_app_db
db.postgresql.username=postgres
db.postgresql.password=13579.,ad

# Connection Pool Configuration
db.pool.initialSize=5
db.pool.maxSize=20
db.pool.connectionTimeout=30000
db.pool.idleTimeout=600000
db.pool.maxLifetime=1800000

# API Configuration (for future backend integration)
api.base.url=http://localhost:8080/api
api.key=YOUR_API_KEY_HERE

# Chapa Test Checkout Configuration
# Set CHAPA_SECRET_KEY in your environment for real checkout requests.
payment.chapa.secretKey=
payment.chapa.initializeUrl=https://api.chapa.co/v1/transaction/initialize
payment.chapa.verifyUrl=https://api.chapa.co/v1/transaction/verify
payment.chapa.returnUrl=https://chapa.co/
payment.chapa.currency=ETB
payment.chapa.title=EthioTour
